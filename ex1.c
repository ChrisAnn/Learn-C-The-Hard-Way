#include <stdio.h>

int main()
{
    puts("Hello world.");
    puts("The sky above the port was the color of television, tuned to a dead channel.");
    puts("I don't want to waste time looking for 5 more lines of text");
    puts("I find that I'm easily distracted by 'easier' tasks in order to avoid the 'tougher' ones");
    puts("So, instead, I hope that this rambling will suffice.");
    
    return 0;
}
